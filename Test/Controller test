package com.citi.rock.gwy.api.pts.controller;

import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.content;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import java.util.Collections;

import com.citi.rock.gwy.api.pts.dto.GlobalFilterRequestDto;
import com.citi.rock.gwy.api.pts.dto.TopCardRequestDto;
import com.citi.rock.gwy.api.pts.dto.TopcardResponseDto;
import com.citi.rock.gwy.api.pts.service.PtsDashboardService;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import org.junit.jupiter.api.extension.ExtendWith;

import com.fasterxml.jackson.databind.ObjectMapper;

@ExtendWith(MockitoExtension.class)
class PtsDashboardControllerTest {

    private MockMvc mockMvc;

    @InjectMocks
    private PtsDashboardController controller;

    @Mock
    private PtsDashboardService service;

    private ObjectMapper objectMapper;

    @BeforeEach
    void setup() {
        mockMvc = MockMvcBuilders.standaloneSetup(controller).build();
        objectMapper = new ObjectMapper();
    }

    @Test
    void testGetGlobalFilters() throws Exception {
        when(service.getGlobalFilters()).thenReturn(Collections.emptyList());

        mockMvc.perform(org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get("/pts/getFilters")
                        .contentType(MediaType.APPLICATION_JSON))
               .andExpect(status().isOk())
               .andExpect(content().json("[]"));
    }

    @Test
    void testGetTopcard1() throws Exception {
        TopCardRequestDto request = new TopCardRequestDto();
        request.setGlobalFilters(new GlobalFilterRequestDto());
        TopcardResponseDto response = new TopcardResponseDto();
        when(service.getRagStatus(request.getGlobalFilters())).thenReturn(response);

        mockMvc.perform(post("/pts/topcard1")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(objectMapper.writeValueAsString(request)))
               .andExpect(status().isOk())
               .andExpect(content().json(objectMapper.writeValueAsString(response)));
    }

    @Test
    void testGetTopcard2() throws Exception {
        TopCardRequestDto request = new TopCardRequestDto();
        request.setGlobalFilters(new GlobalFilterRequestDto());
        TopcardResponseDto response = new TopcardResponseDto();
        when(service.getTopcard2ByRBCM(request.getGlobalFilters())).thenReturn(response);

        mockMvc.perform(post("/pts/topcard2")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(objectMapper.writeValueAsString(request)))
               .andExpect(status().isOk())
               .andExpect(content().json(objectMapper.writeValueAsString(response)));
    }

    @Test
    void testGetTopcard3() throws Exception {
        TopCardRequestDto request = new TopCardRequestDto();
        request.setGlobalFilters(new GlobalFilterRequestDto());
        TopcardResponseDto response = new TopcardResponseDto();
        when(service.getTopcard3(request.getGlobalFilters())).thenReturn(response);

        mockMvc.perform(post("/pts/topcard3")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(objectMapper.writeValueAsString(request)))
               .andExpect(status().isOk())
               .andExpect(content().json(objectMapper.writeValueAsString(response)));
    }
}












package com.citi.rock.gwy.api.pts.controller;

import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.content;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import java.util.Collections;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import com.citi.rock.gwy.api.pts.service.PtsDashboardService;

@ExtendWith(MockitoExtension.class)
class PtsDashboardControllerTest {

    private MockMvc mockMvc;

    @InjectMocks
    private PtsDashboardController controller;

    @Mock
    private PtsDashboardService service;

    @BeforeEach
    void setup() {
        // Create standalone MockMvc with controller + mocks
        mockMvc = MockMvcBuilders.standaloneSetup(controller).build();
    }

    @Test
    void testGetGlobalFilters_shouldReturnOk() throws Exception {
        // Arrange: mock service response
        when(service.getGlobalFilters()).thenReturn(Collections.emptyList());

        // Act & Assert: call controller endpoint
        mockMvc.perform(get("/pts/getFilters")
                .contentType(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk())
                .andExpect(content().contentType(MediaType.APPLICATION_JSON))
                .andExpect(content().json("[]"));
    }
}







package com.citi.rock.gwy.api.pts;

import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.content;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import java.util.Collections;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import com.citi.rock.gwy.api.pts.controller.PtsDashboardController;
import com.citi.rock.gwy.api.pts.service.PtsDashboardService;

@ExtendWith(MockitoExtension.class)
class PtsDashboardControllerTest {

    private MockMvc mockMvc;

    @InjectMocks
    private PtsDashboardController controller;

    @Mock
    private PtsDashboardService service;

    @BeforeEach
    void setup() {
        mockMvc = MockMvcBuilders.standaloneSetup(controller).build();
    }

    @Test
    void testGetGlobalFilters() throws Exception {
        // Mock service response
        when(service.getGlobalFilters()).thenReturn(Collections.emptyList());

        // Call controller endpoint and verify
        mockMvc.perform(get("/pts/getFilters")
                        .contentType(MediaType.APPLICATION_JSON))
               .andExpect(status().isOk())
               .andExpect(content().json("[]"));
    }
}










class PtsDashboardControllerTest {

    private MockMvc mockMvc;

    @InjectMocks
    private PtsDashboardController controller;

    @Mock
    private PtsDashboardService service;

    @BeforeEach
    void setup() {
        mockMvc = MockMvcBuilders.standaloneSetup(controller).build();
    }

    @Test
    void testGetGlobalFilters() throws Exception {
        when(service.getGlobalFilters()).thenReturn(Collections.emptyList());

        mockMvc.perform(get("/pts/getFilters")
                        .contentType(MediaType.APPLICATION_JSON))
               .andExpect(status().isOk())
               .andExpect(content().json("[]"));
    }
}



import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;

import java.util.Collections;

import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@WebMvcTest(PtsDashboardController.class)
class PtsDashboardControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private PtsDashboardService service;

    @Test
    void getGlobalFilters_shouldReturnOk() throws Exception {
        // Mock the service
        when(service.getGlobalFilters()).thenReturn(Collections.emptyList());

        // Perform GET request
        mockMvc.perform(get("/pts/getFilters"))
                .andExpect(status().isOk());
    }
}









import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import java.util.Collections;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;

// your package imports
import com.yourpackage.controller.PtsDashboardController;
import com.yourpackage.service.PtsDashboardService;

@WebMvcTest(PtsDashboardController.class)
class PtsDashboardControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private PtsDashboardService service;

    @Test
    void getGlobalFilters_shouldReturnOk() throws Exception {

        // mock service response
        when(service.getGlobalFilters())
                .thenReturn(Collections.emptyList());

        // call controller endpoint
        mockMvc.perform(get("/pts/getFilters"))
                .andExpect(status().isOk());
    }
}








package com.citi.rock.gwy.api.pts.controller;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

import java.util.Collections;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;

import com.citi.rock.gwy.api.pts.service.PtsDashboardService;

import static org.mockito.Mockito.*;

@WebMvcTest(PtsDashboardController.class)
class PtsDashboardControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private PtsDashboardService service;

    @Test
    void shouldReturnFilters() throws Exception {
        when(service.getGlobalFilters()).thenReturn(Collections.emptyList());

        mockMvc.perform(get("/pts/getFilters"))
               .andExpect(status().isOk());
    }

    @Test
    void shouldReturnTopcard1() throws Exception {
        when(service.getRagStatus(any())).thenReturn(new TopcardResponseDto());

        mockMvc.perform(post("/pts/topcard1")
                .contentType(MediaType.APPLICATION_JSON)
                .content("{}"))
               .andExpect(status().isOk());
    }

    @Test
    void shouldReturnTopcard2() throws Exception {
        when(service.getTopcard2ByRBCM(any())).thenReturn(new TopcardResponseDto());

        mockMvc.perform(post("/pts/topcard2")
                .contentType(MediaType.APPLICATION_JSON)
                .content("{}"))
               .andExpect(status().isOk());
    }

    @Test
    void shouldReturnTopcard3() throws Exception {
        when(service.getTopcard3(any())).thenReturn(new TopcardResponseDto());

        mockMvc.perform(post("/pts/topcard3")
                .contentType(MediaType.APPLICATION_JSON)
                .content("{}"))
               .andExpect(status().isOk());
    }
}
